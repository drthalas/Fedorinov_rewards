# Legacy Workspace

Legacy source repositories are cloned into `legacy/_external/` for analysis only.

That directory is ignored by git so old WinForms code is not mixed into the new project repository.
