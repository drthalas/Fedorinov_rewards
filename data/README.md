# Local Data Placeholder

Do not store real Rewards data in this repository.

Use `REWARDS_DATA_DIR` to point the application to the local external data folder, for example:

```sh
REWARDS_DATA_DIR=/Users/hermes/Desktop/Rewards
```

SQLite databases, photos, generated PDFs, archives, and binary files are intentionally ignored by git.
